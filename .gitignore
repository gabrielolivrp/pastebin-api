.env
# Local .terraform directories
**/.terraform/

# .tfstate files and backups (contain sensitive state information)
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Terraform variable files (may contain sensitive data like passwords or API keys)
*.tfvars
*.tfvars.json

# Ignore override files, as they are often used for local development overrides
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Terraform CLI configuration files
.terraformrc
terraform.rc

# Terraform plan files (if you output plans to a file)
# Example: *tfplan*

# macOS specific files
.DS_Store
